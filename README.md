# RAG-project-
DocuQuery AI is a Retrieval-Augmented Generation (RAG) system that enables semantic search and question answering over user-uploaded documents. It processes PDFs, generates embeddings, retrieves relevant context using a vector database, and produces accurate responses using a local language model.
